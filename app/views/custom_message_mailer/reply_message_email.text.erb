새로운 답장
===============================================

 <%=@message.sender.name %> 님으로 부터 답장이 왔습니다

-----------------------------------------------
<%= @message.body.html_safe? ? @message.body : strip_tags(@message.body) %>
-----------------------------------------------

<%= link_to("확인하러 가기", conversations_path ) %>
